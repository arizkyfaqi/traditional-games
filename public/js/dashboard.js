const btnDelete = document.querySelector('.delete-user');
const btnEdit = document.querySelector('.edit-user');

const deleteUser = async () => {};
btnEdit.addEventListener('click', (e) => {});
btnDelete.addEventListener('click', (e) => {
  deleteUser();
});
